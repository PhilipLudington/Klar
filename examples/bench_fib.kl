// bench_fib.kl - Fibonacci benchmark

fn fib(n: i32) -> i32 {
    if n <= 1 {
        return n
    } else {
        return fib(n - 1) + fib(n - 2)
    }
}

fn main() -> i32 {
    let result: i32 = fib(30)
    println("Done")
    return 0
}
