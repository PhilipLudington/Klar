// hello.kl - Hello World in Klar

fn add(a: i32, b: i32) -> i32 {
    return a + b
}

fn main() -> i32 {
    let x: i32 = 10
    let y: i32 = 20
    let sum: i32 = add(x, y)
    println("Hello, Klar!")
    return sum - 30  // Returns 0 for success
}
