// test_while.kl - Test while loop for native compilation

fn main() -> i32 {
    var count: i32 = 0
    var n: i32 = 10

    while n > 0 {
        count = count + 1
        n = n - 1
    }

    // count should be 10
    return count
}
