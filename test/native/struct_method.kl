// Test for struct methods (impl blocks)

struct Point {
    x: i32,
    y: i32,
}

impl Point {
    fn sum(self: Point) -> i32 {
        self.x + self.y
    }
}

fn main() -> i32 {
    let p = Point { x: 10, y: 32 }
    p.sum()
}
