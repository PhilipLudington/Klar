// Test for builtin Clone trait - integer

fn main() -> i32 {
    // Test integer cloning
    let a: i32 = 42
    let b: i32 = a.clone()

    // b should be a copy of a with the same value
    if a.eq(b) {
        // Values are equal - correct!
        return 0
    } else {
        // Values differ - should not happen
        return 1
    }
}
