// Test len builtin with string
fn main() -> i32 {
    let s: string = "hello"
    let length: i32 = len(s)
    // "hello" has 5 characters - return 0 if length is 5
    if length == 5 {
        return 0
    } else {
        return 1
    }
}
