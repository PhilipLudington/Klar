// Test: klar build bare_metal_target.kl --target aarch64-none-elf --freestanding -c
// Expected: Compiles to ELF object file without linking

// Entry point for bare-metal - returns i32 for compatibility
fn _start() -> i32 {
    // Minimal bare-metal code
    return 0
}
