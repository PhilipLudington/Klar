// Test generic enum pattern matching
// Note: Match expression codegen is not yet implemented.
// This test verifies that generic enum patterns can be parsed and type-checked.
// Expected exit: 0 (match codegen returns placeholder)
// Will return 100 once match codegen is implemented.

enum MyOption[T] {
    MySome(T),
    MyNone,
}

fn main() -> i32 {
    // Test with None value
    let none_val = MyOption[i32]::MyNone

    // Pattern match with generic enum type syntax
    let result1 = match none_val {
        MyOption.MySome(_) => 0,
        MyOption.MyNone => 100,
    }

    result1
}
