// Test returning a closure from a function
// With explicit type annotations on closure parameter
fn make_adder(n: i32) -> fn(i32) -> i32 {
    return |x: i32| -> i32 { return x + n }
}

fn main() -> i32 {
    let add5: fn(i32) -> i32 = make_adder(5)
    return add5(10)  // Should return 15
}
