// Test: meta hint annotation on a function
meta hint("prefer calling with positive values")
fn abs(x: i32) -> i32 {
    if x < 0 {
        return 0 - x
    }
    return x
}

fn main() -> i32 {
    if abs(-5) == 5 and abs(3) == 3 {
        println("ok")
    }
    return 0
}
