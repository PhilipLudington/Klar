// Test: custom annotation with test scope
meta define test_category(category: string) for test

fn add(a: i32, b: i32) -> i32 {
    return a + b
}

meta test_category("unit")
test add {
    assert_eq(add(1, 2), 3)
}

fn main() -> i32 {
    return 0
}
