// Test: Iterator and IntoIterator traits are defined as builtins
// Expected exit code: 42
// This test verifies the traits are registered in the type system
// by using them as generic bounds.

// This function signature verifies Iterator trait exists with Item associated type
fn count_items[T: Iterator](iter: T) -> i32 {
    // Can't actually implement without codegen support for &mut self
    // But this verifies the trait bound is valid
    return 0
}

fn main() -> i32 {
    // The generic function above compiled, meaning Iterator is a valid trait
    return 42
}
