// Test importing tuple with [char]

import lib.tuple_char_array.{ State, init }

fn main() -> i32 {
    let pair: (State, [char]) = init("hello")
    println("Len: {pair.0.len}")
    println("First: {pair.1[0]}")
    return 0
}
